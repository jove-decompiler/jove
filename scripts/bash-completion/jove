_jove()
{
    local cur=${COMP_WORDS[COMP_CWORD]}
    COMPREPLY=()
    if [[ "$COMP_CWORD" == "1" ]]; then
      COMPREPLY=( $(compgen -W "check-helper dump-vdso gen-tcgconstants add addr2off analyze block bootstrap callstack cfg decompile dig dump extract function ida init invalidate ipt llvm loop numbers off2addr readtrace recompile recover run sanity scan score serialize server stub trace trace2addrs trace2asm trace2lines unlock unserialize unstub jv2xml llknife tcgdump" -- $cur) )
    else
      _filedir
    fi
}
complete -F _jove jove
